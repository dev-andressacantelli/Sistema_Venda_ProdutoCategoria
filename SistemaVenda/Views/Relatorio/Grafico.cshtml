@{
    ViewData["Title"] = "Gráfico";
}

<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.bundle.min.js"></script>

<h2>Total de Vendas por Produto</h2>
<hr />

<canvas id="myChart"></canvas>

<script>
    var ctx = document.getElementById('myChart').getContext('2d');
    var chart = new Chart(ctx, {
        type: 'pie',

        data: {
            labels: [@Html.Raw(ViewBag.Labels)], //NOMES
            datasets: [{
                backgroundColor: [@Html.Raw(ViewBag.Cores) ], //CORES
                borderColor: '#f5f5f5',
                data: [@Html.Raw(ViewBag.Valores)] //QUANTIDADE
            }]
        },

        options: {}
    });
</script>

<!-- //MOOCADO
        data: {
            labels: ['Coca-Cola', 'Finni', 'Trakinas'],
            datasets: [{
                backgroundColor: ['#E83845', '#E389B9', '#7464B0'],
                borderColor: '#f5f5f5',
                data: [10, 15, 20]
            }]
        },
-->
